{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Michael Giannakopoulos - Personal Portfolio with Blog and Dynamic Projects",
  "main": "index.js",
  "scripts": {
    "build": "node build-blog.js",
    "build-blog": "node build-blog.js",
    "build-projects": "node build-blog.js",
    "serve": "python -m http.server 8000 --bind 127.0.0.1",
    "dev": "npm run build && python -m http.server 8000 --bind 127.0.0.1",
    "dev-win": "npm run build && python -m http.server 8000",
    "watch": "nodemon --watch blog-content --watch portfolio-content --ext md,json --exec \"npm run build\"",
    "clean": "rm -rf posts/* api/*",
    "setup": "mkdir -p blog-content/posts blog-content/templates portfolio-content/projects assets/images api posts"
  },
  "keywords": ["portfolio", "blog", "projects", "static-site-generator"],
  "author": "Michael Giannakopoulos",
  "license": "ISC",
  "dependencies": {
    "marked": "^9.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}